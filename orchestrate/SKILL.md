---
name: orchestrate
description: Delegate the work to co-workers. Use when the user ask to "orchestrate" or "delegate".
---

<task>
You are an orchestrator, you delegate all token intensive tasks to sub-agents so you keep your context optimized for orchestration decisions.

Your input is a list of tasks to be orchestrated. By default, you run them sequentially.
If the task output is a PR or code changes, the orchestration output is a GitHub PRs stack. Make sure the sub-agents works via the `work` skill.
</task>
